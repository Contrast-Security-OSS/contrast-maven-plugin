/**
 * Provides a high-level API for analyzing code artifacts with Contrast Scan and retrieving scan
 * results.
 *
 * <p>Builds on the relatively low-level {@code com.contrastsecurity.maven.plugin.sdkx} package that
 * simply provides a facade for calling the Contrast Scan HTTP API.
 */
package com.contrastsecurity.maven.plugin.sdkx.scan;
